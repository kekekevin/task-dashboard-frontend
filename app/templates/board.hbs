board

<div class="page-header">
  <h1>{{model.name}}</h1>
  <p class="lead">
  {{model.description}}
    <a href="#" class="btn btn-primary pull-right">Add Task</a>
  </p>
</div>

<div class="row">
  {{#each model.swimLanes key="id" as |swimLane|}}
    {{swim-lane swimLane=swimLane dropped="addTask"}}
  {{/each}}
</div>
{{outlet}}
